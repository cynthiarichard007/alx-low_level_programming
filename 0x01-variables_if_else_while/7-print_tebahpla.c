#include <stdio.h>

/**
 * main - Prints alphabets at reverse
 *
 * Return: Always(Sucessful)
 */
int main(void)
{
char c;
for (c = 'z' ; c <= 'a' ; c++)
{
        putchar(c);
}
putchar('\n');
return(0);
}
